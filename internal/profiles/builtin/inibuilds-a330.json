{
  "description": "IniBuilds A330. Reads the data points below from the aircraft's own variables instead of the stock ones.",
  "id": "inibuilds-a330",
  "mash": {
    "apu.switchOn": {
      "bindings": [
        {
          "sim": "simconnect",
          "source": {
            "kind": "lvar",
            "name": "INI_APU_MASTER_SWITCH"
          },
          "transform": [
            {
              "op": "bool"
            }
          ]
        },
        {
          "sim": "simconnect",
          "source": {
            "kind": "simvar",
            "name": "APU SWITCH",
            "unit": "Bool"
          },
          "transform": [
            {
              "op": "bool"
            }
          ]
        }
      ],
      "reduce": "or"
    }
  },
  "match": {
    "all": [
      {
        "field": "simulator",
        "op": "equals",
        "value": "simconnect"
      },
      {
        "field": "aircraftName",
        "op": "matches",
        "value": "ini\\s*-?builds?|inisim"
      },
      {
        "field": "aircraftName",
        "op": "matches",
        "value": "a[\\s-]*330"
      }
    ]
  },
  "name": "IniBuilds A330",
  "notes": "Generated by `go run ./cmd/hubhop gen` from the MobiFlight HubHop community database. The selector and the bindings have not been confirmed in the simulator: treat this as a starting point, and correct it in place once flown. Every binding falls back to the stock variable, and a local variable the aircraft does not define is withheld rather than read as zero, so a wrong name costs nothing.",
  "priority": 50,
  "x-generated": true
}
