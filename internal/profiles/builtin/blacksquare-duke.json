{
  "description": "Black Square Duke. Reads the data points below from the aircraft's own variables instead of the stock ones.",
  "id": "blacksquare-duke",
  "mash": {
    "autopilot.approachHold": {
      "bindings": [
        {
          "sim": "simconnect",
          "source": {
            "kind": "lvar",
            "name": "var_APModes_Approach_EM_readonly"
          },
          "transform": [
            {
              "op": "bool"
            }
          ]
        },
        {
          "sim": "simconnect",
          "source": {
            "kind": "lvar",
            "name": "var_APPanel_Approach_EM_readonly"
          },
          "transform": [
            {
              "op": "bool"
            }
          ]
        },
        {
          "sim": "simconnect",
          "source": {
            "kind": "simvar",
            "name": "AUTOPILOT APPROACH HOLD",
            "unit": "Bool"
          },
          "transform": [
            {
              "op": "bool"
            }
          ]
        }
      ],
      "reduce": "or"
    }
  },
  "match": {
    "all": [
      {
        "field": "simulator",
        "op": "equals",
        "value": "simconnect"
      },
      {
        "field": "aircraftName",
        "op": "matches",
        "value": "black\\s*-?square"
      },
      {
        "field": "aircraftName",
        "op": "matches",
        "value": "duke"
      }
    ]
  },
  "name": "Black Square Duke",
  "notes": "Generated by `go run ./cmd/hubhop gen` from the MobiFlight HubHop community database. The selector and the bindings have not been confirmed in the simulator: treat this as a starting point, and correct it in place once flown. Every binding falls back to the stock variable, so an unread aircraft variable costs nothing.",
  "priority": 50,
  "x-generated": true
}
